# Core analysis + tests. CPU wheels are enough for the offline suite.
numpy>=1.26
pyyaml>=6.0
pytest>=8.0

# Needed for the GPU / model-dependent scripts (Hessian capture, QAT/KD pilots).
# On ROCm install torch from the ROCm index instead of PyPI, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
# The exact pilot-node snapshot is recorded in requirements-gpu-lock.txt.
# Qwen3.5/Qwen3.8 support requires the newer Transformers line (5.5+ in the
# recorded environment); the legacy Qwen3 path remains compatible with >=4.40.
torch>=2.0
transformers>=4.40
datasets>=2.18
